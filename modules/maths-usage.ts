import Message, { pi as π, π2, phi, absolute } from './maths.js'

import * as m from './maths.js'

console.log(π2)
const absPhi = absolute(-phi);
console.log(absPhi);

const message = new Message()
console.log(message.month)

console.log('math', m)

